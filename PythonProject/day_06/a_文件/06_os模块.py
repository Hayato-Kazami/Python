"""
Python 中对文件、文件夹的管理操作，都可以通过内置 os 模块实现。
使用前必须先导入：import os
1. os.rename (旧名称，新名称)：重命名文件 / 文件夹，也可用于移动文件。
2. os.remove (文件名)：删除指定文件（无法删除文件夹）。
3. os.mkdir (文件夹名)：创建新的文件夹（目录）。
4. os.getcwd()：获取当前程序所在的目录路径。
5. os.chdir (目标目录)：切换当前工作目录。
6. os.listdir (目标目录)：获取指定目录下的所有文件和子目录，返回列表。
7. os.rmdir (目标目录)：删除空文件夹（必须为空才能删除）。
"""
"""
适用目录结构：
|--- day06
      |--- a_文件
            |--- g
      |--- t
            |--- x
"""

# 1. 重命名文件（把 day06 下的 1.txt → 111.txt）


# 2. 删除文件（删除 g 目录下的 2.txt）


# 3. 创建新文件夹（在 day06 下创建 test 文件夹）


# 4. 获取当前工作目录


# 5. 切换目录（切换到 t 目录）


# 6. 查看指定目录下的所有文件（查看 day06 目录）


# 7. 删除空文件夹（删除 x 空目录）
